# DataTable Example

库的本地演示工程（Expo SDK 57）。Metro 已配置为把 `@bestcoder/react-native-data-table` 直接解析到上级目录的 `src/`，修改库源码即时热更新，无需 build。

## 运行

```bash
cd example
npm install
npm run ios      # 或 npm run android / npm run web
```
